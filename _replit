language = "python3"
run = "python -m app --host \"${ADDRESS:-0.0.0.0}\" --port \"${EXPOSE_PORT:-5000}\""
#run = "killall -q python3 > /dev/null 2>&1; pip install -r requirements.txt && python3 -m pip install --upgrade pip && sh ./run"
#onBoot = "killall -q python3 > /dev/null 2>&1; pip install -r requirements.txt && python3 -m pip install --upgrade pip && sh ./run"
